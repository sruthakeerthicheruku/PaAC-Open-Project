### Technical Manual
* Setup: Install Python 3.9+ and run pip install torch in the terminal.

* Execution: Run the script using python src/main.py.

* Validation: The model saves weights to outputs/model.pt and prints fidelity metrics to the console.